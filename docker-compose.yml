services:
  jotl-companion:
    image: ghcr.io/sinimus/jotl-companion:latest
    container_name: jotl-companion
    ports:
      - "8080:80"
    restart: unless-stopped
    # No volumes needed as data is stored in the browser's IndexedDB
